# AUTOPROTECTTMN — демо-версия сайта

Статический сайт, собирать нечего: `index.html` лежит в корне.
Это **демо для показа заказчику** — 10 роликов из 32.
Полная версия собирается в основном проекте: `bash build-dist.sh`.

Страницы: `index.html`, `services.html`, `works.html`, `404.html`.
